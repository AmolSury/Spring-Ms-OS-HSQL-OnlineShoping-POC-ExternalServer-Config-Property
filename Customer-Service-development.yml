server:
  port: 5051
db:
  name: customerdb
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

ui:
  serverUrl: http://localhost:4200/
management:
  endpoints:
    web:
      exposure:
        include: '*'